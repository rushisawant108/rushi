name: rushi
description: A new Flutter project.

publish_to: 'none' # Prevents accidental publishing
version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  table_calendar: ^3.0.8
  fl_chart: ^0.66.2        # Charting library
  animate_do: ^3.0.2       # Animation library
  pdf: ^3.10.4
  printing: ^5.12.0
  excel: ^2.1.0
  path_provider: ^2.1.1
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
